Day-1
Question:Every year, the city of Whoville conducts a Reindeer Run to find the best reindeers for Santa's Sleigh. Can you write a query to return the name and rank of the top 7 reindeers
in this race?
*Tables:*  reindeer_run_results(number, name, rank, color)
SOLUTION:

select name, rank
from reindeer_run_results
  order by rank limit 7

Day -2
Question: Santa wants to analyze which toys that were produced in his workshop have already been delivered to children. You are given two tables on toy production and toy delivery — can you
return the toy ID and toy name of the toys that have been delivered?

Tables:
toy_production(toy_id, toy_name, production_date)
toy_delivery(toy_id, child_name, delivery_date)
SOLUTION:

SELECT d.toy_id, p.toy_name
FROM toy_delivery d 
INNER JOIN toy_production p 
on d.toy_id = p.toy_id;

Day -3
Question:The Grinch has brainstormed a ton of pranks for Whoville, but he only wants to keep the top prank per target, with the highest evilness score. Return the most evil prank
for each target. If two pranks have the same evilness, the more recently brainstormed wins.

Tables
grinch_prank_ideas(prank_id, target_name, prank_description, evilness_score, created_at)
SOLUTION:
WITH cte AS(
SELECT prank_id, target_name,prank_description,evilness_score,
    rank() over(partition by target_name order by evilness_score desc, created_at desc)
  AS rnk
FROM grinch_prank_ideas)
SELECT target_name, 
  prank_description,
  evilness_score
  FROM cte 
  WHERE rnk =1

Day -4
Question: Kevin's trying to decorate the house without sending the electricity bill through the roof. Write a query to find the top 5 most energy-efficient decorations 
(i.e. lowest cost per hour to operate).
Tables
hall_decorations(decoration_id, decoration_name, energy_cost_per_hour)
SOLUTION:

select decoration_id, decoration_name,
energy_cost_per_hour
from hall_decorations
order by energy_cost_per_hour
limit 5

Day -5
Question: 






